# Инструкция по деплою на GitHub Pages

Этот проект настроен для работы как на локальном сервере (для разработки), так и на GitHub Pages (для продакшена).

## 🚀 Быстрый старт

### 1. Подготовка репозитория

1. Создайте репозиторий на GitHub (если еще не создан)
2. Загрузите код в репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
   git push -u origin main
   ```

### 2. Настройка GitHub Pages

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе `Source` выберите:
   - Branch: `main` (или `master`)
   - Folder: `/ (root)`
3. Нажмите `Save`
4. GitHub автоматически запустит деплой через GitHub Actions

### 3. Настройка Firebase (опционально, для облачного бекенда)

Для полноценной работы бекенда на GitHub Pages рекомендуется настроить Firebase:

1. Создайте проект на [Firebase Console](https://console.firebase.google.com/)
2. Включите следующие сервисы:
   - **Firestore Database** (для хранения данных)
   - **Storage** (для загрузки файлов)
   - **Authentication** (опционально, для авторизации)

3. Получите конфигурацию Firebase:
   - Перейдите в `Project Settings` → `General` → `Your apps`
   - Добавьте веб-приложение
   - Скопируйте конфигурацию

4. Обновите `firebase-config.js`:
   ```javascript
   const firebaseConfig = {
       apiKey: "ВАШ_API_KEY",
       authDomain: "ВАШ_PROJECT_ID.firebaseapp.com",
       projectId: "ВАШ_PROJECT_ID",
       storageBucket: "ВАШ_PROJECT_ID.appspot.com",
       messagingSenderId: "ВАШ_MESSAGING_SENDER_ID",
       appId: "ВАШ_APP_ID"
   };
   ```

5. Настройте правила безопасности Firestore:
   ```javascript
   // Правила для чтения (публичные)
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /{document=**} {
         allow read: if true;
         allow write: if request.auth != null; // Только авторизованные пользователи
       }
     }
   }
   ```

6. Настройте правила Storage:
   ```javascript
   rules_version = '2';
   service firebase.storage {
     match /b/{bucket}/o {
       match /{allPaths=**} {
         allow read: if true;
         allow write: if request.auth != null;
       }
     }
   }
   ```

### 4. Работа без Firebase (только localStorage)

Если вы не хотите настраивать Firebase, сайт будет работать с использованием localStorage браузера. Однако:
- ⚠️ Данные будут храниться только в браузере пользователя
- ⚠️ Изменения не будут синхронизироваться между пользователями
- ⚠️ Данные могут быть потеряны при очистке кэша

## 📝 Локальная разработка

Для локальной разработки используйте Express сервер:

```bash
# Установка зависимостей
npm install

# Запуск сервера
npm start

# Сервер будет доступен на http://localhost:3000
```

Админ-панель автоматически определит, что вы используете локальный сервер, и подключится к Express API.

## 🔄 Как это работает

### Автоматическое определение окружения

Админ-панель автоматически определяет окружение:

1. **Локальная разработка** (`localhost` или `127.0.0.1`):
   - Используется Express API (`/api/*`)
   - Данные хранятся в SQLite базе данных
   - Полный функционал бекенда

2. **GitHub Pages** (любой другой домен):
   - Используется Firebase Backend (если настроен)
   - Или localStorage fallback
   - Данные синхронизируются через Firebase

### Структура файлов

```
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions для автоматического деплоя
├── firebase-config.js          # Конфигурация Firebase
├── firebase-backend.js         # Firebase Backend сервис
├── admin-panel-backend.html    # Админ-панель (автоматически определяет окружение)
├── server.js                   # Express сервер (только для локальной разработки)
└── ...                         # Остальные файлы сайта
```

## ✅ Проверка работы

После деплоя:

1. Откройте ваш сайт на GitHub Pages: `https://ВАШ_USERNAME.github.io/ВАШ_РЕПОЗИТОРИЙ/`
2. Проверьте админ-панель: `https://ВАШ_USERNAME.github.io/ВАШ_РЕПОЗИТОРИЙ/admin-panel-backend.html`
3. Войдите с учетными данными:
   - Логин: `admin`
   - Пароль: `admin123` (для localStorage) или ваш пароль из Firebase

## 🔧 Решение проблем

### Ошибка "Порт 3000 уже занят"

Если при запуске `npm start` вы видите ошибку `EADDRINUSE`:
- Остановите уже запущенный сервер (Ctrl+C)
- Или измените порт в `server.js`: `const PORT = process.env.PORT || 3001;`

### Данные не сохраняются на GitHub Pages

- Проверьте, что Firebase настроен правильно
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что правила безопасности Firestore разрешают запись

### GitHub Actions не деплоит

- Проверьте, что файл `.github/workflows/deploy.yml` существует
- Убедитесь, что в настройках репозитория включен GitHub Pages
- Проверьте логи в разделе `Actions` вашего репозитория

## 📚 Дополнительная информация

- [Документация GitHub Pages](https://docs.github.com/en/pages)
- [Документация Firebase](https://firebase.google.com/docs)
- [GitHub Actions](https://docs.github.com/en/actions)

## 🎉 Готово!

Теперь ваш сайт работает на GitHub Pages, и все изменения будут видны всем пользователям!

